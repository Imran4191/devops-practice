name: First Workflow

on:
  workflow_dispatch:

jobs:
  First_Job:
    runs-on: ubuntu-latest
    
    steps:
      - name: First Step
        run: |
          echo "This is the first step of the first job."
